# Environment / secrets
.env
*.env
.env.local

# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
env/
*.egg-info/
dist/
build/

# Reports are intentionally tracked — COOKBOOK.md must persist across runs
# (local and cloud Automation) so each weekly run can build on the last one.

# macOS
.DS_Store

# IDE
.cursor/
*.cursor-tutor
